<div class="profile-page">
	<div id="video-screenshot">
		<video src="<%= @user.video.url %>" controls="controls" width="300" height="300">
			your browser does not support the video tag
		</video>
	</div>
	<div class="user-name">
		<%= @user.name %>
	</div>
</div>