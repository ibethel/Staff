<% if user.video.url.index(".png").to_i == 0 %>
<video src="<%= user.video.url %>" controls="controls" width="300" height="300">
	your browser does not support the video tag
</video>
<% else %>
	<p><%= user.name %> has not yet uploaded a video</p>
<% end %>