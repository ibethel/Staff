<h1>Departments</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @departments.each do |department| %>
  <tr>
    <td><%= department.name %></td>
    <td><%= link_to 'Edit', edit_admin_department_path(department) %></td>
    <td><%= link_to 'Remove', admin_department_path(department), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>