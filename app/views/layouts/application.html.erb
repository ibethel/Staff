<!DOCTYPE html>
<html>
<head>
	<title>Bethel Church Staff | Who are We?</title>
	<link href='http://fonts.googleapis.com/css?family=Josefin+Sans|Droid+Sans' rel='stylesheet' type='text/css'>
	<%= stylesheet_link_tag :all, cache: "all-staff" %>
	<%= javascript_include_tag :defaults, "random", "form.jquery", "jquery.remotipart", "jquery.scroll", cache: "all-staff" %>
	<%= csrf_meta_tag %>
</head>
<body>
	<div id="wrapper">
		<h1 id="logo"><%= link_to "Bethel Church Staff", root_path %></h1>
		<div id="authentication">
			<% if current_user %>
				<%= link_to "My Profile", profile_path(current_user) %> | <%= link_to "Sign Out", signout_path %>
			<% else %>
				<%= link_to "Sign in", "/auth/google_apps" %>
			<% end %>
		</div>
		
		<% if flash[:notice] %>
			<div id="messages">
			<%= flash[:notice] %>
			</div>
		<% end%>
		
		<%= yield %>
	</div>
</body>
</html>