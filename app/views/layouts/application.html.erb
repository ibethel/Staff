<!DOCTYPE html>
<html>
<head>
	<title>Bethel Church Staff</title>
	<link href='http://fonts.googleapis.com/css?family=Kristi' rel='stylesheet' type='text/css'>
	<%= stylesheet_link_tag :all, cache: "all-staff" %>
	<%= javascript_include_tag :defaults, cache: "all-staff" %>
	<%= csrf_meta_tag %>
</head>
<body>
	<div id="wrapper">
		<div id="authentication">
			<% if current_user %>
				Welcome, <%= link_to current_user.name, profile_path(current_user) %> | <%= link_to "Sign Out", signout_path %>
			<% else %>
				<%= link_to "Sign in", "/auth/google_apps" %>
			<% end %>
		</div>
	
		<%= yield %>
	</div>
</body>
</html>